This directory holds the generated source evaluation report (docs/source_evaluation.md),
which is regenerated on every pipeline run and excluded from git via .gitignore.
A live sample of the output is embedded in README.md.